---
layout: layouts/home.njk
eleventyNavigation:
  key: Home
  order: 1
---

<div class="container">
  <div class="row">
    <div class="col-12 text-center p-3">
      <h1>Hi There, I'm Sam.</h1><br>
      <h3>A Web Developer, Python Enthusiast and Problem Solver.</h3>
    </div>
  </div>
  <div class="row justify-content-center">
    <div class="col-12 col-lg-5 text-center p-3 sb-token m-3">
      {% set maxPosts = collections.posts.length | min(1) %}
      <h5>Latest {% if maxPosts == 1 %}Post{% else %}{{ maxPosts }} Posts{% endif %}</h5>

      {% set postslist = collections[ "BLOG" ] | head(-1) %}
      {% set postslistCounter = collections.posts | length %}
      {% include "postslist.njk" %}
    </div>

    <div class="col-12 col-lg-5 text-center p-3 sb-token m-3">
      <h5>Latest Project</h5>

      {% set postslist = collections[ "PROJECT" ] | head(-1) %}
      {% set postslistCounter = collections.posts | length %}
      {% include "postslist.njk" %}
    </div>

  </div>
</div>
